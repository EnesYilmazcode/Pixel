# Core
fastapi
uvicorn[standard]
python-multipart
python-dotenv
pillow
numpy
scipy
google-genai

# Vision — real DeepGaze IIE (active). For CPU-only machines install torch first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch
git+https://github.com/matthias-k/DeepGaze.git

# Memory / observability — Phase 2 (uncomment when wiring Scout/Pinecone + LangSmith)
# pinecone
# langsmith
