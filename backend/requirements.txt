# Core
fastapi
uvicorn[standard]
python-multipart
python-dotenv
pillow
numpy
scipy
google-genai

# Vision — real DeepGaze IIE (active). For CPU-only machines install torch first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch
git+https://github.com/matthias-k/DeepGaze.git
# DeepGaze IIE feature extractors need these (open-source, no API keys, weights cached at runtime):
einops
timm
git+https://github.com/openai/CLIP.git

# Agent orchestration — LangChain (LCEL) drives the Director. No API key / network
# needed. (Pulls langsmith transitively; tracing stays off unless a key is set.)
langchain-core

# Memory — Pinecone (Scout uses it when PINECONE_API_KEY is set; else local kb.json)
# pinecone
