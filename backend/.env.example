# Backend secrets template. Copy to backend/.env and fill in. .env is git-ignored.
GEMINI_API_KEY=
PINECONE_API_KEY=
PINECONE_INDEX=pixel-ads

# LangSmith (optional — tracing + attention_score chart). No-op if key blank.
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=pixel
