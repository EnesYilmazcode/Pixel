# public/

Static assets served at the site root.

Drop the landing-page demo here:
- `demo.mp4` — the walkthrough video (`<source src="/demo.mp4">`)
- `demo-poster.png` — still frame shown before play (`poster="/demo-poster.png"`)

Until both exist, the landing shows a "Demo video" placeholder frame.
